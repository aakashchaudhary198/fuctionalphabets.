#include<stdio.h>
void alpha(char);
void alpha(char ch)
{
	switch(ch)
	case'a':
		case'e':
			case'i':
				case'o':
					case'u':
		printf(" is vowel");
		defalt:
			printf("is content");
}
int main()
 {
 	char ch;
 	 printf(" enter the alphabets");
 	 scanf("%c",&ch);
 	 alpha(ch);
 	 return 0;
 }
